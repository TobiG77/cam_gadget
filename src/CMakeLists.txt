cmake_minimum_required(VERSION 2.8)
project( hit-or-miss )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( hit-or-miss hit-or-miss.cpp )
target_link_libraries( hit-or-miss ${OpenCV_LIBS} )